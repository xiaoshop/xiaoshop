# AGENTS.md

## 项目概览

本仓库目前处于**非常早期的开发阶段**。欢迎提出有助于提升长期可维护性的大规模改动。

## 任务完成要求

- 在认为任务完成之前，`nr lint` 和 `nr typecheck` 必须全部通过。

## 核心优先级

1. 性能优先。
2. 可靠性优先。

如果需要权衡取舍，请选择正确性和健壮性，而非短期便利。

## 可维护性

长期可维护性是核心优先级。如果新增功能，请先检查是否存在可以提取到独立模块的共享逻辑。跨多个文件的重复逻辑是不被允许的。不要害怕修改现有代码。不要为了解决问题而只是在本地添加逻辑、走捷径。
